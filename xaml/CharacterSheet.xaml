<!-- Character Sheet - TTRPG Plugin for XamlToy -->
<!-- View at: https://xamltoy.noesisengine.com -->
<Grid
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="#0D0D1A">

    <Grid.Resources>
        <!-- Color Palette -->
        <SolidColorBrush x:Key="PrimaryBg" Color="#1a1a2e"/>
        <SolidColorBrush x:Key="SecondaryBg" Color="#2a2a4e"/>
        <SolidColorBrush x:Key="GoldAccent" Color="#ccb588"/>
        <SolidColorBrush x:Key="PrimaryText" Color="#cccccc"/>
        <SolidColorBrush x:Key="SecondaryText" Color="#888888"/>
        <SolidColorBrush x:Key="HelpText" Color="#666666"/>
        <SolidColorBrush x:Key="AbilityLabel" Color="#96a9be"/>
        <SolidColorBrush x:Key="Success" Color="#4caf50"/>
        <SolidColorBrush x:Key="Error" Color="#f44336"/>

        <!-- Typography Styles -->
        <Style x:Key="PageTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource GoldAccent}"/>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource GoldAccent}"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="AbilityLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{StaticResource AbilityLabel}"/>
            <Setter Property="Width" Value="35"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Button Styles -->
        <Style x:Key="DndSecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2a2a4e"/>
            <Setter Property="Foreground" Value="#aaaaaa"/>
            <Setter Property="BorderBrush" Value="#4a4a6e"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DndSmallButton" TargetType="Button">
            <Setter Property="Background" Value="#2a2a4e"/>
            <Setter Property="Foreground" Value="#aaaaaa"/>
            <Setter Property="BorderBrush" Value="#4a4a6e"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Width" Value="45"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DiceButton" TargetType="Button">
            <Setter Property="Background" Value="#2a2a4e"/>
            <Setter Property="Foreground" Value="#cccccc"/>
            <Setter Property="BorderBrush" Value="#4a4a6e"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="55"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,0,5,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HpControlButton" TargetType="Button">
            <Setter Property="Background" Value="#2a2a4e"/>
            <Setter Property="Foreground" Value="#aaaaaa"/>
            <Setter Property="BorderBrush" Value="#4a4a6e"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Grid.Resources>

    <!-- Main Container (simulating $C.@PageOverlay + $C.@Container) -->
    <Border Background="#D91a1a2e" Width="700" Height="550" Padding="20"
            HorizontalAlignment="Center" VerticalAlignment="Center"
            BorderBrush="#4a4a6e" BorderThickness="1" CornerRadius="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Section -->
            <StackPanel Grid.Row="0" Margin="0,0,0,15">
                <TextBlock x:Name="Title" Text="Character Sheet" Style="{StaticResource PageTitle}"/>
                <TextBlock x:Name="PlayerName" Text="Sir Galahad the Brave" Foreground="#aaaaaa" FontSize="14" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Content: Three Columns -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- ABILITY SCORES COLUMN -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="ABILITY SCORES" Style="{StaticResource SectionHeader}"/>

                    <!-- STR -->
                    <StackPanel Orientation="Horizontal" Height="35" Margin="0,0,0,5">
                        <TextBlock Text="STR" Style="{StaticResource AbilityLabelStyle}"/>
                        <TextBlock x:Name="strScore" Text="16" FontSize="18" FontWeight="Bold" Width="30" VerticalAlignment="Center"/>
                        <TextBlock x:Name="strMod" Text="+3" FontSize="12" Foreground="{StaticResource Success}" Width="30" VerticalAlignment="Center"/>
                        <Button x:Name="strRoll" Content="Roll" Style="{StaticResource DndSmallButton}"/>
                        <Button x:Name="strSave" Content="Save" Style="{StaticResource DndSmallButton}" Margin="3,0,0,0"/>
                    </StackPanel>

                    <!-- DEX -->
                    <StackPanel Orientation="Horizontal" Height="35" Margin="0,0,0,5">
                        <TextBlock Text="DEX" Style="{StaticResource AbilityLabelStyle}"/>
                        <TextBlock x:Name="dexScore" Text="14" FontSize="18" FontWeight="Bold" Width="30" VerticalAlignment="Center"/>
                        <TextBlock x:Name="dexMod" Text="+2" FontSize="12" Foreground="{StaticResource Success}" Width="30" VerticalAlignment="Center"/>
                        <Button x:Name="dexRoll" Content="Roll" Style="{StaticResource DndSmallButton}"/>
                        <Button x:Name="dexSave" Content="Save" Style="{StaticResource DndSmallButton}" Margin="3,0,0,0"/>
                    </StackPanel>

                    <!-- CON -->
                    <StackPanel Orientation="Horizontal" Height="35" Margin="0,0,0,5">
                        <TextBlock Text="CON" Style="{StaticResource AbilityLabelStyle}"/>
                        <TextBlock x:Name="conScore" Text="12" FontSize="18" FontWeight="Bold" Width="30" VerticalAlignment="Center"/>
                        <TextBlock x:Name="conMod" Text="+1" FontSize="12" Foreground="{StaticResource Success}" Width="30" VerticalAlignment="Center"/>
                        <Button x:Name="conRoll" Content="Roll" Style="{StaticResource DndSmallButton}"/>
                        <Button x:Name="conSave" Content="Save" Style="{StaticResource DndSmallButton}" Margin="3,0,0,0"/>
                    </StackPanel>

                    <!-- INT -->
                    <StackPanel Orientation="Horizontal" Height="35" Margin="0,0,0,5">
                        <TextBlock Text="INT" Style="{StaticResource AbilityLabelStyle}"/>
                        <TextBlock x:Name="intScore" Text="10" FontSize="18" FontWeight="Bold" Width="30" VerticalAlignment="Center"/>
                        <TextBlock x:Name="intMod" Text="+0" FontSize="12" Foreground="{StaticResource PrimaryText}" Width="30" VerticalAlignment="Center"/>
                        <Button x:Name="intRoll" Content="Roll" Style="{StaticResource DndSmallButton}"/>
                        <Button x:Name="intSave" Content="Save" Style="{StaticResource DndSmallButton}" Margin="3,0,0,0"/>
                    </StackPanel>

                    <!-- WIS -->
                    <StackPanel Orientation="Horizontal" Height="35" Margin="0,0,0,5">
                        <TextBlock Text="WIS" Style="{StaticResource AbilityLabelStyle}"/>
                        <TextBlock x:Name="wisScore" Text="13" FontSize="18" FontWeight="Bold" Width="30" VerticalAlignment="Center"/>
                        <TextBlock x:Name="wisMod" Text="+1" FontSize="12" Foreground="{StaticResource Success}" Width="30" VerticalAlignment="Center"/>
                        <Button x:Name="wisRoll" Content="Roll" Style="{StaticResource DndSmallButton}"/>
                        <Button x:Name="wisSave" Content="Save" Style="{StaticResource DndSmallButton}" Margin="3,0,0,0"/>
                    </StackPanel>

                    <!-- CHA -->
                    <StackPanel Orientation="Horizontal" Height="35" Margin="0,0,0,5">
                        <TextBlock Text="CHA" Style="{StaticResource AbilityLabelStyle}"/>
                        <TextBlock x:Name="chaScore" Text="8" FontSize="18" FontWeight="Bold" Width="30" VerticalAlignment="Center"/>
                        <TextBlock x:Name="chaMod" Text="-1" FontSize="12" Foreground="{StaticResource Error}" Width="30" VerticalAlignment="Center"/>
                        <Button x:Name="chaRoll" Content="Roll" Style="{StaticResource DndSmallButton}"/>
                        <Button x:Name="chaSave" Content="Save" Style="{StaticResource DndSmallButton}" Margin="3,0,0,0"/>
                    </StackPanel>
                </StackPanel>

                <!-- COMBAT INFO COLUMN -->
                <StackPanel Grid.Column="1" Margin="15,0,0,0">
                    <TextBlock Text="HIT POINTS" Style="{StaticResource SectionHeader}"/>

                    <!-- HP Display -->
                    <StackPanel Orientation="Horizontal" Height="50" Margin="0,0,0,10">
                        <TextBlock x:Name="CurrentHp" Text="24" FontSize="36" FontWeight="Bold" Foreground="{StaticResource Success}" Width="60"/>
                        <TextBlock Text="/" FontSize="28" Foreground="{StaticResource HelpText}" Width="20" VerticalAlignment="Bottom" Margin="0,0,0,5"/>
                        <TextBlock x:Name="MaxHp" Text="30" FontSize="28" Foreground="{StaticResource SecondaryText}" Width="50" VerticalAlignment="Bottom" Margin="0,0,0,3"/>
                    </StackPanel>

                    <!-- HP Controls -->
                    <StackPanel Orientation="Horizontal" Height="35" Margin="0,0,0,15">
                        <Button x:Name="hpMinus5" Content="-5" Style="{StaticResource HpControlButton}"/>
                        <Button x:Name="hpMinus" Content="-1" Style="{StaticResource HpControlButton}" Margin="3,0,0,0"/>
                        <Button x:Name="hpPlus" Content="+1" Style="{StaticResource HpControlButton}" Margin="3,0,0,0"/>
                        <Button x:Name="hpPlus5" Content="+5" Style="{StaticResource HpControlButton}" Margin="3,0,0,0"/>
                    </StackPanel>

                    <!-- Armor Class -->
                    <TextBlock Text="ARMOR CLASS" FontSize="12" FontWeight="Bold" Foreground="{StaticResource GoldAccent}" Margin="0,10,0,5"/>
                    <TextBlock x:Name="ArmorClass" Text="16" FontSize="28" FontWeight="Bold" Foreground="{StaticResource PrimaryText}" Margin="0,0,0,15"/>

                    <!-- Roll Initiative Button -->
                    <Button x:Name="rollInitiative" Content="Roll Initiative" Style="{StaticResource DndSecondaryButton}" Width="150"/>
                </StackPanel>

                <!-- DICE ROLLER COLUMN -->
                <StackPanel Grid.Column="2" Margin="15,0,0,0">
                    <TextBlock Text="DICE ROLLER" FontSize="12" FontWeight="Bold" Foreground="{StaticResource GoldAccent}" Margin="0,0,0,15"/>

                    <!-- Dice Buttons -->
                    <WrapPanel x:Name="DiceButtons" Orientation="Horizontal">
                        <Button x:Name="rollD4" Content="D4" Style="{StaticResource DiceButton}"/>
                        <Button x:Name="rollD6" Content="D6" Style="{StaticResource DiceButton}"/>
                        <Button x:Name="rollD8" Content="D8" Style="{StaticResource DiceButton}"/>
                        <Button x:Name="rollD10" Content="D10" Style="{StaticResource DiceButton}"/>
                        <Button x:Name="rollD12" Content="D12" Style="{StaticResource DiceButton}"/>
                        <Button x:Name="rollD20" Content="D20" Style="{StaticResource DiceButton}"/>
                    </WrapPanel>

                    <!-- Combat Status -->
                    <StackPanel x:Name="CombatStatus" Margin="0,15,0,0">
                        <TextBlock x:Name="combatStatusLabel" Text="Combat Active - Round 3" FontSize="11" Foreground="{StaticResource SecondaryText}" Height="16"/>
                        <TextBlock x:Name="turnStatusLabel" Text="Your turn!" FontSize="11" Foreground="{StaticResource Success}" Height="16"/>

                        <StackPanel Orientation="Horizontal" Height="40" Margin="0,10,0,0">
                            <Button x:Name="endTurnBtn" Content="End Turn" Style="{StaticResource DndSecondaryButton}" Width="80" Margin="0,0,5,0"/>
                            <Button x:Name="openCombatBtn" Content="Combat" Style="{StaticResource DndSecondaryButton}" Width="80" Margin="5,0,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Grid>
